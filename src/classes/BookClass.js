class BookClass {
  constructor(id, title, authors, thumbnail, shelf) {
    this.id = id;
    this.title = title;
    this.authors = authors;
    this.thumbnail = thumbnail;
    this.shelf = shelf;
  }
}

export default BookClass;
